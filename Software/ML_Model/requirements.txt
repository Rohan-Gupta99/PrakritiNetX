# Training environment - needs Python 3.11 (TensorFlow does not support 3.14).
#   pip install uv
#   uv python install 3.11
#   uv venv .venv --python 3.11
#   uv pip install --python .venv/Scripts/python.exe -r requirements.txt
tensorflow==2.15.1
numpy<2
pillow
